defmodule Test.CssSelectors do
  # def navbar_link(), do: "#app-navbar-main a"
  # def profile_menu_link(), do: "#profile-menu a"
  def page_heading(), do: "h1"
  def toast(), do: ".toast"
  # def main(), do: "main"
end
